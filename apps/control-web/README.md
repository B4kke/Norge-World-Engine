# apps/control-web

Reserved module boundary for Norge World Engine.

This directory is intentionally minimal during Prototype 0. Do not add framework or engine dependencies until the relevant requirement is testable and the choice is documented in `docs/04-decisions.md`.
