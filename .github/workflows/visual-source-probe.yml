name: visual-source-probe

on:
  pull_request:
    paths:
      - 'tools/visual-data/**'
      - 'docs/data-licenses/visual-sources.md'
      - '.github/workflows/visual-source-probe.yml'
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Syntax check
        run: |
          python -m py_compile tools/visual-data/probe_visual_sources.py
          python -m py_compile tools/visual-data/probe_visual_order_selections.py
          python -m py_compile tools/visual-data/probe_sr16_metadata_links.py
          python -m py_compile tools/visual-data/probe_vegetation_real_sample.py

      - name: Probe current official source contracts
        run: |
          python tools/visual-data/probe_visual_sources.py --output /tmp/nwe-visual-source-probe.json
          python tools/visual-data/probe_visual_order_selections.py --output /tmp/nwe-visual-order-selection.json
          python tools/visual-data/probe_sr16_metadata_links.py --output /tmp/nwe-sr16-metadata-links.json
          python tools/visual-data/probe_vegetation_real_sample.py --output /tmp/nwe-vegetation-real-source-probe.json
          cat /tmp/nwe-visual-source-probe.json
          cat /tmp/nwe-visual-order-selection.json
          cat /tmp/nwe-sr16-metadata-links.json
          cat /tmp/nwe-vegetation-real-source-probe.json

      - uses: actions/upload-artifact@v4
        with:
          name: visual-source-probe-${{ github.sha }}
          path: |
            /tmp/nwe-visual-source-probe.json
            /tmp/nwe-visual-order-selection.json
            /tmp/nwe-sr16-metadata-links.json
            /tmp/nwe-vegetation-real-source-probe.json
          if-no-files-found: error
          retention-days: 14
